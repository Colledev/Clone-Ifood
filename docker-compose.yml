version: "3"

services:
  postgres:
    image: postgres:latest
    container_name: clone-ifood
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./.postgres-data:/var/lib/postgressql/data

volumes:
  .postgres-data:
